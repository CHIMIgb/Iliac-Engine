{
  "name": "raycast-studio",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "setup:textures": "node -e \"const fs=require('fs');const d='public/textures';fs.mkdirSync(d,{recursive:true});for(const f of fs.readdirSync('../demo/textures'))fs.copyFileSync('../demo/textures/'+f,d+'/'+f);console.log('Texturas copiadas a public/textures');\""
  },
  "dependencies": {
    "three": "^0.185.1"
  },
  "devDependencies": {
    "@types/three": "^0.185.0",
    "typescript": "^5.7.0",
    "vite": "^6.3.0",
    "vitest": "^3.2.0"
  }
}
